guard 'coffeescript', :output => 'public/js' do
  watch(%r{app/js/(.+\.coffee)})
end

guard 'compass' do
  watch(%r{^app/css/(.*)\.s[ac]ss})
end

guard 'livereload' do
  watch(%r{.+\.(js|css|html)})
end
